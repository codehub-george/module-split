import { ShortenTextDirective } from './shorten-text.directive';

describe('ShortenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortenTextDirective();
    expect(directive).toBeTruthy();
  });
});
