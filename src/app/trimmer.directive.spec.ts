import { TrimmerDirective } from './trimmer.directive';

describe('TrimmerDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimmerDirective();
    expect(directive).toBeTruthy();
  });
});
